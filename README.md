# Homelab Server

- **Platform**: TRUENAS-MINI-XL+
- **Edition**: Community
- **Version**: Goldeye

The repository manages the `compose.yml` files for deploying stacks in Portainer.
`.env` files are ignored to hide secrets.
